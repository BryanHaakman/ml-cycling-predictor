---
phase: 04.1-guest-api-pivot
plan: "01"
subsystem: data/odds
tags:
  - cleanup
  - revert
  - experiment-removal
dependency_graph:
  requires: []
  provides:
    - clean-codebase-without-playwright-experiment
  affects:
    - data/odds.py
    - webapp/app.py
    - requirements.txt
    - .gitignore
    - tests/test_odds.py
tech_stack:
  added: []
  patterns:
    - inline-constants-in-module (no config.py indirection)
key_files:
  created: []
  modified:
    - data/odds.py
    - webapp/app.py
    - requirements.txt
    - .gitignore
    - tests/test_odds.py
  deleted:
    - config.py
    - .env.example
    - data/session_manager.py
    - tests/test_config.py
    - tests/test_session_manager.py
decisions:
  - "Reverted data/odds.py to inline constants (PINNACLE_API_BASE, PINNACLE_CYCLING_SPORT_ID, etc.) — no config.py indirection"
  - "Removed playwright and python-dotenv from requirements.txt — guest API needs no Playwright"
  - "Removed .env and session state from .gitignore — no longer applicable"
metrics:
  duration: "~8 minutes"
  completed: "2026-04-13"
  tasks_completed: 2
  files_changed: 10
---

# Phase 04.1 Plan 01: Revert Playwright Session Manager Experiment Summary

**One-liner:** Deleted all Playwright session manager experiment artifacts (config.py, session_manager.py, test files) and reverted data/odds.py, webapp/app.py, requirements.txt, .gitignore, tests/test_odds.py to pre-04.1 state using inline constants.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Delete experiment files and revert modified files | 5fa8ff9 | config.py (del), .env.example (del), data/session_manager.py (del), tests/test_config.py (del), tests/test_session_manager.py (del), data/odds.py, webapp/app.py, requirements.txt, .gitignore, tests/test_odds.py |
| 2 | Full test suite verification | (no code changes) | — |

## Verification Results

- `from data.odds import fetch_cycling_h2h_markets, OddsMarket, PinnacleAuthError` — import OK
- `pytest tests/test_odds.py -v` — 25/25 passed
- `PINNACLE_API_BASE == 'https://api.arcadia.pinnacle.com/0.1'` — Constant OK
- No `session_manager`, `from config import`, or `dotenv` references in any .py file
- playwright and python-dotenv absent from requirements.txt
- .env and session state entries absent from .gitignore

## Deviations from Plan

### Pre-existing Issues (Out of Scope)

**test_name_resolver.py failures (9 tests)**
- **Found during:** Task 2 full test suite run
- **Issue:** NameResolver tests require rider data in data/cache.db, but worktree has empty cache.db (0 riders). These failures exist regardless of Plan 01's changes.
- **Root cause:** Worktree environment — no database with rider data. Tests call `resolver.resolve("Primož Roglič")` which queries riders table in SQLite.
- **Fix:** Not fixed — out of scope. These are environment-level failures unrelated to the experiment revert.
- **Deferred to:** These tests pass in the main repo where cache.db has 292K+ pairs. Not a Plan 01 regression.

### Worktree Branch Rebase

The worktree was created from `main` (which had newer commits) instead of the target base 497dcde. Applied `git reset --soft 497dcde` to rebase to the correct starting point, then staged only Plan 01's 10 files. All other diverging files (from newer main commits) remain unstaged for the orchestrator to handle.

## Known Stubs

None — this plan only deletes/reverts code; no new functionality introduced.

## Threat Flags

None — deletion and revert only; no new trust boundaries introduced.

## Self-Check: PASSED

- [x] Task 1 commit exists: 5fa8ff9 (chore(04.1-01): delete Playwright session manager experiment artifacts)
- [x] All 5 experiment files deleted from disk: config.py, .env.example, data/session_manager.py, tests/test_config.py, tests/test_session_manager.py
- [x] data/odds.py has inline constants, no config import, no session_manager
- [x] webapp/app.py has no dotenv import
- [x] requirements.txt has no playwright or python-dotenv
- [x] .gitignore has no .env or session state entries
- [x] PINNACLE_API_BASE == 'https://api.arcadia.pinnacle.com/0.1'
- [x] pytest tests/test_odds.py — 25 passed
