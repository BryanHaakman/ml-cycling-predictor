---
phase: 04.1-playwright-session-manager
plan: "01"
subsystem: config-and-env
tags: [config, dotenv, credentials, playwright, test-scaffold]
dependency_graph:
  requires: []
  provides: [config.py, .env.example, dotenv-wired, test-scaffolds]
  affects: [data/odds.py, webapp/app.py, tests/]
tech_stack:
  added: [python-dotenv==1.2.2, playwright==1.58.0]
  patterns: [centralized-config, env-file-secrets, dotenv-no-override]
key_files:
  created:
    - config.py
    - .env.example
    - tests/test_config.py
    - tests/test_session_manager.py
  modified:
    - data/odds.py
    - webapp/app.py
    - .gitignore
    - requirements.txt
decisions:
  - "config.py uses _ROOT = os.path.dirname(os.path.abspath(__file__)) for absolute path resolution — avoids CWD-relative paths"
  - "load_dotenv() placed before all data imports in app.py — ensures env vars available before any module-level os.environ.get() call"
  - "data/odds.py imports from config — removes local constant duplication (D-06 compliance)"
metrics:
  duration_minutes: 15
  completed_date: "2026-04-13"
  tasks_completed: 2
  tasks_total: 2
  files_created: 4
  files_modified: 4
---

# Phase 04.1 Plan 01: Config, .env Infrastructure, and Test Scaffolds Summary

**One-liner:** Centralized config.py with migrated Pinnacle constants, python-dotenv wired into Flask startup, .env.example committed, and test scaffolds for session_manager ready for Plan 02.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create config.py, .env infrastructure, and update dependencies | c3bf371 | config.py, .env.example, .gitignore, requirements.txt, webapp/app.py, data/odds.py |
| 2 | Create test scaffolds for session_manager and config modules | 1a905e9 | tests/test_config.py, tests/test_session_manager.py |

## Verification

- `python -c "from config import PINNACLE_API_BASE, SESSION_STATE_PATH"` exits 0
- `python -c "from data.odds import fetch_cycling_h2h_markets"` exits 0 (no regression)
- `pytest tests/test_config.py -v` — 9 passed
- `pytest tests/test_odds.py -v` — 25 passed (no regression from constant migration)
- `grep "^.env$" .gitignore` — present
- `grep "load_dotenv" webapp/app.py` — present before blueprint registration
- Full test suite: 101 passed, 9 pre-existing failures in test_name_resolver.py (require cache.db with rider data, unrelated to this plan)

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — this plan creates infrastructure only (config constants, env template, test scaffolds). No UI-facing data flows.

## Threat Surface Scan

Threat mitigations from plan's threat model applied as required:

| T-ID | Applied | Evidence |
|------|---------|----------|
| T-04.1-01 | Yes | `.env` added to `.gitignore`; `.env.example` contains only placeholders |
| T-04.1-02 | Yes | `data/.pinnacle_session_state.json` added to `.gitignore` |
| T-04.1-03 | Accept | config.py contains only non-secret constants |
| T-04.1-04 | Yes | `load_dotenv()` called before any import that reads `os.environ.get()` |

No new threat surface introduced beyond what the plan's threat model covers.

## Self-Check: PASSED

- config.py: FOUND
- .env.example: FOUND
- tests/test_config.py: FOUND
- tests/test_session_manager.py: FOUND
- Commits c3bf371 and 1a905e9: FOUND (git log confirmed)
