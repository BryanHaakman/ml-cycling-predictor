---
phase: 04.1-guest-api-pivot
plan: "02"
subsystem: data/odds
tags:
  - pinnacle
  - guest-api
  - odds-client
  - tdd
dependency_graph:
  requires:
    - clean-codebase-without-playwright-experiment
  provides:
    - guest-api-odds-client
    - the-field-filter
    - sport-level-2-call-fetch
  affects:
    - data/odds.py
    - tests/test_odds.py
tech_stack:
  added: []
  patterns:
    - sport-level-2-call-api-pattern (matchups + markets, no per-league loop)
    - the-field-participant-filter
    - tdd-red-green (tests written first, then implementation)
key_files:
  created: []
  modified:
    - data/odds.py
    - tests/test_odds.py
decisions:
  - "PINNACLE_API_BASE changed to guest.api.arcadia.pinnacle.com — public, no auth required"
  - "fetch_cycling_h2h_markets() simplified from 1+2N per-league calls to 2 sport-level calls"
  - "PINNACLE_SESSION_COOKIE env var removed from _get_api_key() lookup chain — guest API needs no session"
  - "The Field participant filter added — 5 of 19 live matchups are outright bets, not H2H"
  - "race_name sourced from matchup.league.name (embedded in matchup object, no separate leagues call)"
  - "X-Api-Key retained as courtesy header — not enforced by guest endpoints but matches browser behavior"
metrics:
  duration: "~15 minutes"
  completed: "2026-04-13"
  tasks_completed: 2
  files_changed: 2
---

# Phase 04.1 Plan 02: Guest API Client Summary

**One-liner:** Changed PINNACLE_API_BASE to guest.api.arcadia.pinnacle.com, rewrote fetch to 2 sport-level calls, added The Field filter, removed all session token machinery, and added 13 new tests covering all new behavior.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Update data/odds.py — guest API constant, sport-level fetch, The Field filter | 9a870c2 | data/odds.py, tests/test_odds.py |
| 2 | Full test suite green + import verification | (no code changes) | — |

## Verification Results

- `from data.odds import PINNACLE_API_BASE; assert 'guest.api.arcadia' in PINNACLE_API_BASE` — PASSED
- `PINNACLE_API_BASE == 'https://guest.api.arcadia.pinnacle.com/0.1'` — PASSED
- `pytest tests/test_odds.py -v` — 38/38 passed (25 existing + 13 new)
- `pytest tests/ -v` — 90 passed, 9 failed (9 pre-existing test_name_resolver.py failures requiring cache.db rider data — not regressions)
- `from data.odds import OddsMarket, PinnacleAuthError, fetch_cycling_h2h_markets` — import OK
- `import webapp.pinnacle_bp` — import OK
- Zero `session_manager` references in data/odds.py or tests/test_odds.py
- Zero `PINNACLE_SESSION_COOKIE` references in data/odds.py

## New Test Classes

| Class | Tests | Behavior Covered |
|-------|-------|-----------------|
| TestGuestApiConstant | 3 | PINNACLE_API_BASE contains guest.api.arcadia.pinnacle.com, uses HTTPS, not auth subdomain |
| TestTheFieldFilter | 3 | rider_a The Field excluded, rider_b The Field excluded, mixed matchups (Field excluded, H2H kept) |
| TestSportLevelFetch | 3 | Exactly 2 HTTP calls, first call is /sports/45/matchups, second is /sports/45/markets/straight |
| TestNoSessionHeader | 2 | No X-Session header in any request, X-Api-Key IS present |
| TestRaceNameFromLeague | 4 | race_name from matchup.league.name, empty matchups returns [], non-open market skipped, <2 participants skipped |

## Deviations from Plan

### Auto-fixed Issues

None — plan executed exactly as written. TDD approach followed: RED (12 failing tests written first), then GREEN (implementation added, all 38 pass).

### Existing Test Updates

The plan requested updating `TestGetApiKey` to remove `PINNACLE_SESSION_COOKIE` references. The existing `test_returns_env_var_when_set` test was replaced with `test_returns_cache_when_available` — removing the env var priority path entirely from the test, matching the new `_get_api_key()` implementation.

The `TestFetchCyclingH2hMarkets` class was rewritten to use the 2-call mock pattern (matchups_resp + markets_resp) instead of the old 3-call pattern (leagues_resp + matchups_resp + markets_resp). The auth retry tests were simplified to mock `_get_api_key` directly.

## Known Stubs

None — the implementation is fully wired. The guest API client makes real HTTP calls in production and the mock pattern in tests is correctly isolated.

## Threat Flags

None — no new trust boundaries introduced beyond what the plan's threat model covers. The guest API response validation (list check, participants count, market status, prices designations) mitigates T-04.1-02 (Spoofing). T-04.1-03 (DoS) is mitigated by REQUEST_TIMEOUT=60s and RequestException catch with audit log.

## Self-Check: PASSED

- [x] Task 1 commit exists: 9a870c2 (feat(04.1-02): guest API client — 2-call sport-level fetch, The Field filter, no X-Session)
- [x] PINNACLE_API_BASE == 'https://guest.api.arcadia.pinnacle.com/0.1'
- [x] fetch_cycling_h2h_markets makes exactly 2 HTTP calls (verified by TestSportLevelFetch)
- [x] The Field filter present (verified by TestTheFieldFilter)
- [x] No X-Session header in requests (verified by TestNoSessionHeader)
- [x] X-Api-Key still sent as courtesy header (verified by TestNoSessionHeader)
- [x] race_name from matchup.league.name (verified by TestRaceNameFromLeague)
- [x] PINNACLE_SESSION_COOKIE removed from _get_api_key() (no references in data/odds.py)
- [x] No session_manager import in any file
- [x] pytest tests/test_odds.py — 38 passed
- [x] webapp.pinnacle_bp imports from data.odds cleanly
