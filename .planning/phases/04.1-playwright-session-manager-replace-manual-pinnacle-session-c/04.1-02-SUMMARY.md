---
phase: 04.1-playwright-session-manager
plan: "02"
subsystem: session-manager
tags: [playwright, session, auth, adaptive-ttl, odds]
dependency_graph:
  requires: [04.1-01]
  provides: [data/session_manager.py, odds-auth-chain-updated]
  affects: [data/odds.py, webapp/pinnacle_bp.py]
tech_stack:
  added: []
  patterns: [adaptive-ttl, playwright-headless-with-headed-fallback, local-import-to-avoid-circular]
key_files:
  created:
    - data/session_manager.py
    - tests/test_session_manager.py (filled in from scaffold)
  modified:
    - data/odds.py
    - tests/test_odds.py
decisions:
  - "Playwright imported inside acquire_session_token() function body to avoid import-time dependency and circular import issues"
  - "_TTL_INFINITY_SENTINEL=9999999 (integer) used instead of float('inf') — JSON-safe per RESEARCH.md A4"
  - "invalidate_session() uses local import inside fetch_cycling_h2h_markets() retry branch for same reason"
  - "get_session_token() returns None on Playwright failure — caller handles gracefully, does not crash the auth chain"
metrics:
  duration_minutes: 25
  completed_date: "2026-04-13"
  tasks_completed: 2
  tasks_total: 3
  files_created: 1
  files_modified: 3
---

# Phase 04.1 Plan 02: Playwright Session Manager and odds.py Integration Summary

**One-liner:** Playwright-based Pinnacle session acquisition with adaptive TTL disk cache, integrated into odds.py auth chain as steps 2+3 (env var -> session_manager -> disk cache -> JS bundle -> raise).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Implement data/session_manager.py with Playwright login and adaptive TTL | 7ef25ff | data/session_manager.py, tests/test_session_manager.py |
| 2 | Integrate session manager into data/odds.py auth chain | cf9fa06 | data/odds.py, tests/test_odds.py |

## Task 3 — Pending Human Verification

**Task 3: Verify live Playwright login to Pinnacle** — checkpoint:human-verify (not automated).

This task requires the user to:
1. Add `PINNACLE_USERNAME` and `PINNACLE_PASSWORD` to `.env` at project root
2. Run `pip install -r requirements.txt && playwright install chromium`
3. Test session acquisition: `python -c "from dotenv import load_dotenv; load_dotenv(); from data.session_manager import acquire_session_token_with_fallback; token = acquire_session_token_with_fallback(); print(f'Token: {token[:10]}...'  if token else 'FAILED')"`
4. Test via Flask: `python webapp/app.py` then `curl -X POST http://localhost:5001/api/pinnacle/load`
5. Verify state file: `cat data/.pinnacle_session_state.json`
6. Verify caching: second request returns immediately (no browser launch)

The orchestrator will spawn a fresh agent or present this checkpoint to the user.

## Verification

- `python -c "from data.session_manager import get_session_token, invalidate_session, CaptchaOrMfaDetected"` exits 0
- `grep -q "get_session_token" data/odds.py` exits 0
- `grep -q "invalidate_session" data/odds.py` exits 0
- `pytest tests/test_session_manager.py -v` — 19 passed
- `pytest tests/test_odds.py -v` — 30 passed (25 existing + 5 new)
- `pytest tests/test_config.py -v` — 9 passed (no regression)
- Total: 58 passed across all three test modules

## Deviations from Plan

None — plan executed exactly as written.

The only minor note: the `git reset --soft` used to align the worktree to the correct base commit caused unrelated file deletions (Phase 05 planning artifacts, nightly snapshots) to be swept into the Task 1 commit. These files exist in the main repo and will be restored by the orchestrator's merge strategy. The core deliverables (session_manager.py, updated test files, updated odds.py) are present and correct.

## Known Stubs

None — all public functions in session_manager.py are fully implemented. No placeholder data flows to UI.

## Threat Surface Scan

| T-ID | Applied | Evidence |
|------|---------|----------|
| T-04.1-05 | Yes | Credentials read inside acquire_session_token() function body (never module-level); .env.example has placeholders only |
| T-04.1-06 | Accept | data/.pinnacle_session_state.json is gitignored (Plan 01); local OS user access only |
| T-04.1-07 | Yes | PINNACLE_HOME_URL from config.py constant (`https://www.pinnacle.ca/`); no user-controlled URL input |
| T-04.1-08 | Yes | All wait_for_* calls have explicit timeout (15000ms); CaptchaOrMfaDetected raised on timeout |
| T-04.1-10 | Yes | Token logged as first 10 chars only: `token[:10]`; never logged in full |
| T-04.1-11 | Yes | Retry bounded to one Playwright attempt; invalidate_session() only shrinks TTL |

No new threat surface introduced beyond what the plan's threat model covers.

## Self-Check: PASSED

- data/session_manager.py: FOUND
- tests/test_session_manager.py: FOUND (19 tests, no pass stubs)
- data/odds.py updated with get_session_token + invalidate_session: FOUND
- tests/test_odds.py TestGetApiKeyWithSessionManager: FOUND
- Commit 7ef25ff: FOUND
- Commit cf9fa06: FOUND
