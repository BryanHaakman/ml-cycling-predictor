---
status: testing
phase: 04.1-guest-api-pivot
source: [04.1-01-SUMMARY.md, 04.1-02-SUMMARY.md]
started: 2026-04-14T00:00:00Z
updated: 2026-04-14T00:00:00Z
---

## Current Test

number: 1
name: Guest API fetches live H2H markets
expected: |
  Run: `python -c "from data.odds import fetch_cycling_h2h_markets; markets = fetch_cycling_h2h_markets(); print(f'{len(markets)} markets'); print(markets[0] if markets else 'EMPTY')"`
  You should see a list of OddsMarket objects fetched from Pinnacle's guest API. Each market has rider_a, rider_b, odds_a, odds_b, race_name, and matchup_id. No auth errors — the guest API requires zero authentication.
awaiting: user response

## Tests

### 1. Guest API fetches live H2H markets
expected: Run `python -c "from data.odds import fetch_cycling_h2h_markets; markets = fetch_cycling_h2h_markets(); print(f'{len(markets)} markets'); print(markets[0] if markets else 'EMPTY')"` — returns OddsMarket objects with rider names, odds, race name, and matchup IDs. No auth error.
result: [pending]

### 2. The Field matchups are filtered out
expected: None of the returned OddsMarket objects should have "The Field" as rider_a or rider_b. Run: `python -c "from data.odds import fetch_cycling_h2h_markets; markets = fetch_cycling_h2h_markets(); field = [m for m in markets if 'The Field' in (m.rider_a, m.rider_b)]; print(f'Field matchups: {len(field)} (should be 0)')"`
result: [pending]

### 3. No session token in requests
expected: The fetch should work without any PINNACLE_SESSION_COOKIE env var set. No X-Session header is sent. If you previously had PINNACLE_SESSION_COOKIE set, unset it and confirm the fetch still works.
result: [pending]

### 4. Flask /api/pinnacle/load endpoint works
expected: Start Flask (`python webapp/app.py`), then run: `curl -X POST http://localhost:5001/api/pinnacle/load` from another terminal. Response should be JSON with `races` array containing race names and `pairs` with rider matchups, odds, and resolution status. HTTP 200.
result: [pending]

### 5. Load from Pinnacle button in UI
expected: Open http://localhost:5001 in browser, go to Batch H2H tab. Click "Load from Pinnacle" button. A race dropdown appears populated with available races. Selecting a race fills in all stage context fields (race name, date, distance, profile, etc.) and adds matchup rows with rider names and odds.
result: [pending]

### 6. Unresolved riders show orange border
expected: If any Pinnacle rider name can't be matched to a PCS rider, that rider's input field should have an orange border. The field is pre-filled with the best candidate guess. Selecting a valid autocomplete suggestion clears the orange border.
result: [pending]

### 7. Refresh Odds updates auto-sourced odds only
expected: After loading from Pinnacle, manually edit one of the odds fields (type a new number). Then click "Refresh Odds". The manually-edited field should keep your value (not overwritten). All other odds fields that you didn't touch should update to latest Pinnacle values.
result: [pending]

### 8. Race switching replaces pairs and stage context
expected: With matchups loaded for one race, switch to a different race in the dropdown. All stage context fields update to the new race. All previous matchup rows are cleared and replaced with the new race's matchups.
result: [pending]

## Summary

total: 8
passed: 0
issues: 0
pending: 8
skipped: 0
blocked: 0

## Gaps

[none yet]
