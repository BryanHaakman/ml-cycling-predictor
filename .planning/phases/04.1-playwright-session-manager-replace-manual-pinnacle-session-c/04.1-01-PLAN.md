---
phase: 04.1-guest-api-pivot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - .env.example
  - data/session_manager.py
  - tests/test_config.py
  - tests/test_session_manager.py
  - data/odds.py
  - webapp/app.py
  - requirements.txt
  - .gitignore
  - tests/test_odds.py
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12

must_haves:
  truths:
    - "config.py, .env.example, data/session_manager.py, tests/test_config.py, tests/test_session_manager.py are deleted from the working tree"
    - "data/odds.py has inline constants (not importing from config.py), no session_manager imports, no X-Session header logic"
    - "webapp/app.py has no dotenv import or load_dotenv() call"
    - "requirements.txt has no playwright or python-dotenv entries"
    - ".gitignore has no .env or session state entries"
    - "tests/test_odds.py has no session_manager references"
    - "All existing tests pass (pytest tests/ -v)"
  artifacts:
    - path: "data/odds.py"
      provides: "Reverted odds module with inline constants"
      contains: "PINNACLE_API_BASE"
    - path: "webapp/app.py"
      provides: "Flask app without dotenv dependency"
    - path: "requirements.txt"
      provides: "Clean dependency list without experiment deps"
    - path: ".gitignore"
      provides: "Clean gitignore without .env entries"
    - path: "tests/test_odds.py"
      provides: "Original odds tests without session_manager references"
  key_links:
    - from: "data/odds.py"
      to: "inline constants"
      via: "module-level UPPER_SNAKE_CASE constants"
      pattern: "PINNACLE_API_BASE.*=.*https://"
---

<objective>
Revert all Playwright session manager experiment artifacts from Phase 04.1.

Purpose: The Playwright approach failed due to GeoComply/reCAPTCHA complexity. The guest API
(`guest.api.arcadia.pinnacle.com`) needs zero authentication, making the entire session manager
infrastructure unnecessary. This plan cleanly removes all experiment code and restores files to
their pre-04.1 state (commit d5460c3), preparing a clean foundation for Plan 02 to implement
the guest API approach.

Output: All experiment files deleted, all modified files reverted to pre-04.1 state, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-playwright-session-manager-replace-manual-pinnacle-session-c/04.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete experiment files and revert modified files to pre-04.1 state</name>
  <files>
    config.py,
    .env.example,
    data/session_manager.py,
    tests/test_config.py,
    tests/test_session_manager.py,
    data/odds.py,
    webapp/app.py,
    requirements.txt,
    .gitignore,
    tests/test_odds.py
  </files>
  <action>
    **Step 1 -- Delete experiment-only files** (these did not exist before 04.1):
    ```
    git rm config.py
    git rm .env.example
    git rm data/session_manager.py
    git rm tests/test_config.py
    git rm tests/test_session_manager.py
    ```

    **Step 2 -- Revert modified files to their pre-04.1 state** using commit d5460c3 as the baseline:
    ```
    git checkout d5460c3 -- data/odds.py
    git checkout d5460c3 -- webapp/app.py
    git checkout d5460c3 -- requirements.txt
    git checkout d5460c3 -- .gitignore
    git checkout d5460c3 -- tests/test_odds.py
    ```

    This restores data/odds.py with its inline constants (PINNACLE_API_BASE, PINNACLE_CYCLING_SPORT_ID,
    REQUEST_TIMEOUT, KEY_CACHE_PATH, ODDS_LOG_PATH, PINNACLE_HOME_URL), removes the `from config import ...`
    line, removes all `session_manager` imports and X-Session logic from `fetch_cycling_h2h_markets()`,
    removes dotenv from webapp/app.py, removes playwright and python-dotenv from requirements.txt,
    and removes .env and session state entries from .gitignore.

    **Step 3 -- Verify no dangling references to deleted modules:**
    ```
    grep -r "session_manager" --include="*.py" .
    grep -r "from config import" --include="*.py" .
    grep -r "test_config" --include="*.py" .
    grep -r "dotenv" --include="*.py" .
    ```
    All four greps must return zero matches (excluding __pycache__ and .planning). If any match is found
    in a file not covered by the revert, fix that file by removing the reference.

    **Step 4 -- Delete stale planning summaries** (the old experiment summaries will be replaced by new ones):
    ```
    rm .planning/phases/04.1-playwright-session-manager-replace-manual-pinnacle-session-c/04.1-01-SUMMARY.md
    rm .planning/phases/04.1-playwright-session-manager-replace-manual-pinnacle-session-c/04.1-02-SUMMARY.md
    ```

    Also delete the `.env` file if it exists (it was created by the experiment and contains no real secrets):
    ```
    rm -f .env
    ```

    And delete the session state file if it exists:
    ```
    rm -f data/.pinnacle_session_state.json
    ```
  </action>
  <verify>
    <automated>cd "B:/ml-cycling-predictor" && python -c "from data.odds import fetch_cycling_h2h_markets, OddsMarket, PinnacleAuthError; print('import OK')" && pytest tests/test_odds.py -v --tb=short</automated>
  </verify>
  <done>
    - config.py, .env.example, data/session_manager.py, tests/test_config.py, tests/test_session_manager.py are deleted
    - data/odds.py matches commit d5460c3 exactly (inline constants, no config import, no session_manager)
    - webapp/app.py has no dotenv import
    - requirements.txt has no playwright or python-dotenv
    - .gitignore has no .env or session state entries
    - tests/test_odds.py matches commit d5460c3 (no session_manager test classes)
    - Old 04.1-01-SUMMARY.md and 04.1-02-SUMMARY.md are deleted
    - `pytest tests/test_odds.py -v` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Full test suite verification</name>
  <files>data/odds.py</files>
  <action>
    Run the full test suite to verify no other tests broke from the revert:
    ```
    pytest tests/ -v --tb=short
    ```

    If any test fails due to importing session_manager, config, or dotenv, fix the import in the
    failing test file. The most likely failure points:
    - Any test file that imports `from config import ...` -- remove that import
    - Any test file that imports `from data.session_manager import ...` -- remove that import
    - Any fixture or conftest that references session_manager -- remove the reference

    After all tests pass, verify the reverted odds.py has the correct pre-04.1 constant value
    (Plan 02 will update it to the guest subdomain):
    ```
    python -c "from data.odds import PINNACLE_API_BASE; assert PINNACLE_API_BASE == 'https://api.arcadia.pinnacle.com/0.1', f'Wrong: {PINNACLE_API_BASE}'; print('Constant OK')"
    ```
  </action>
  <verify>
    <automated>cd "B:/ml-cycling-predictor" && pytest tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Full test suite (pytest tests/ -v) passes with zero failures
    - No dangling references to config.py, session_manager, or dotenv in any Python file
    - PINNACLE_API_BASE is "https://api.arcadia.pinnacle.com/0.1" (pre-04.1 value, Plan 02 changes it)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan only deletes/reverts code; no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | git checkout revert | accept | Reverting to a known good commit (d5460c3) is deterministic; verify with full test suite |
</threat_model>

<verification>
1. `ls config.py .env.example data/session_manager.py tests/test_config.py tests/test_session_manager.py 2>&1` reports all files as not found
2. `grep -r "session_manager\|from config import\|dotenv" --include="*.py" . | grep -v __pycache__ | grep -v .planning` returns zero matches
3. `pytest tests/ -v` all green
4. `git diff d5460c3 -- data/odds.py` shows no diff (exact revert)
</verification>

<success_criteria>
All Playwright session manager experiment code is fully removed. The codebase is in a clean pre-04.1
state with all tests passing. Plan 02 can build on this clean foundation to implement the guest API approach.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-playwright-session-manager-replace-manual-pinnacle-session-c/04.1-01-SUMMARY.md`
</output>
