---
context: phase
phase: 04.1-guest-api-pivot
task: null
total_tasks: null
status: paused_at_uat
last_updated: 2026-04-14T04:00:57.926Z
---

<current_state>
Phase 04.1 (Guest API Pivot) is fully executed, code-reviewed, fixed, and verified.
UAT testing started (8 tests covering phases 04.1 + 05) but blocked on test 1
because the Pinnacle JS bundle extraction no longer works — the API key is not
embedded in any client-side JS bundle.

The user needs to manually extract the X-Api-Key from browser DevTools and save
it to `data/.pinnacle_key_cache` before any live API test can proceed.
</current_state>

<completed_work>

Completed Tasks:
- Plan 04.1-01: Revert Playwright experiment — 5 files deleted, 5 reverted (commit afb31f2)
- Plan 04.1-02: Guest API client — 2-call pattern, The Field filter, 38 tests (commit 0d1ad6f)
- Code review: 8 findings (1 critical, 4 warning, 3 info)
- Code review fix: 5/5 critical+warning findings fixed (commits e6cd3de through fabd157)
- Test fix: Updated 2 tests expecting removed env_var key (commit faf09a7)
- Verification: 15/15 must-haves passed
- Phase marked complete in ROADMAP.md and STATE.md
- Bug fix: Changed JS bundle URL from pinnacle.com to pinnacle.ca (commit 5f72370)
- UAT file created: 04.1-UAT.md with 8 tests (0/8 complete)
</completed_work>

<remaining_work>

- UAT Test 1: Guest API fetches live H2H markets (BLOCKED — needs API key)
- UAT Test 2: The Field matchups filtered out
- UAT Test 3: No session token in requests
- UAT Test 4: Flask /api/pinnacle/load endpoint works
- UAT Test 5: Load from Pinnacle button in UI
- UAT Test 6: Unresolved riders show orange border
- UAT Test 7: Refresh Odds updates auto-sourced odds only
- UAT Test 8: Race switching replaces pairs and stage context
- Milestone completion (all 6 phases done, pending UAT sign-off)
</remaining_work>

<decisions_made>

- Pivoted from Playwright session manager to guest API because GeoComply/reCAPTCHA blocked automated login
- Fixed bundle extraction URL from pinnacle.com to pinnacle.ca (user uses .ca domain)
- JS bundle extraction is fundamentally broken — Pinnacle no longer embeds X-Api-Key in client JS. Need manual key cache or a different extraction strategy.
</decisions_made>

<blockers>
- **API Key Extraction (BLOCKING)**: `_extract_key_from_bundle()` fails because Pinnacle's JS bundles (runtime.js, vendors.js, main.js) do not contain the X-Api-Key string. The key must be manually obtained from browser DevTools and saved to `data/.pinnacle_key_cache`.
</blockers>

## Required Reading (in order)
1. `.planning/phases/04.1-*/04.1-UAT.md` — UAT test plan (8 tests, 0 complete)
2. `data/odds.py` lines 117-184 — `_extract_key_from_bundle()` and key patterns (may need rewrite)
3. `.planning/phases/04.1-*/04.1-REVIEW.md` — 3 Info-level findings remain unfixed

## Infrastructure State
- Flask app: not running (user needs to start `python webapp/app.py` for UI tests)
- Virtual env: `.venv` exists, needs activation for CLI tests
- `data/.pinnacle_key_cache`: does not exist — must be created before any live API call
- All 103 tests pass as of last run

<context>
Session executed phase 04.1 end-to-end: plans, code review, fixes, verification, then started UAT.
First UAT test immediately hit the API key extraction wall. The _extract_key_from_bundle()
function is architecturally broken for the current Pinnacle site — they've moved the API key
out of client-side JS. Consider either:
1. Making the manual cache the primary path (simplest)
2. Investigating if the key appears in HTML meta tags, inline scripts, or XHR responses
3. Using a headless browser to capture the key from actual API requests
Option 1 is recommended — the key rarely changes and manual caching is low-friction.
</context>

<next_action>
Start with: User extracts X-Api-Key from browser DevTools (Network tab, filter for guest.api.arcadia, copy X-Api-Key header), saves to data/.pinnacle_key_cache. Then run `/gsd-resume-work` to continue UAT from test 1.
</next_action>
