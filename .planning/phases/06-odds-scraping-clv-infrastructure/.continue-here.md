---
context: phase
phase: 06-odds-scraping-clv-infrastructure
task: verification
total_tasks: 5 plans complete
status: human_verification_pending
last_updated: 2026-04-18T23:05:00Z
---

<current_state>
Phase 6 execution is COMPLETE — all 5 plans executed across 3 waves, 107 tests pass, VERIFICATION.md created. Phase is awaiting human verification of 3 UI items before it can be marked complete in ROADMAP.md.

A bug was found and fixed: Book Bet was sending empty `stage_url` (Pinnacle flow has no PCS stage URLs). Fix committed in `214f260` — `stage_url` is now optional, backend generates synthetic URL. User needs to restart Flask server to pick up the fix.

User also requested two changes during verification:
1. All matchup rows should be bookable (not just value bets) — done
2. Remove Single H2H mode entirely — done
</current_state>

<completed_work>
All 5 plans complete:
- Plan 06-01: Playwright Pinnacle scraper (data/pinnacle_scraper.py, scripts/scrape_odds.py, scripts/schedule_closing_odds.py) — 21 tests
- Plan 06-02: CLV data layer (data/pnl.py extended with compute_clv, get_total_bankroll, get_clv_summary, get_clv_by_terrain, schema migration) — 22 tests
- Plan 06-03: Flask API layer (webapp/pinnacle_bp.py rewired, CLV/bet/bankroll endpoints added to app.py) — 9 tests
- Plan 06-04: Bet booking UI (webapp/templates/index.html — editable stakes, Book Bet on all rows, snapshot capture)
- Plan 06-05: CLV dashboard (webapp/templates/pnl.html — summary cards, rolling chart, terrain table, per-bet CLV column)

Post-execution fixes:
- Removed stale test_odds.py and test_pinnacle_bp.py (imported deleted data/odds.py)
- Made stage_url optional in /api/pnl/bet (generates synthetic pinnacle/{race-name})
- Made all rows bookable with rider selection prompt for non-value rows
- Removed Single H2H mode per user request
</completed_work>

<remaining_work>
1. User restarts Flask server and verifies Book Bet flow works with stage_url fix
2. User verifies CLV dashboard rendering on /pnl page
3. User verifies snapshot capture and status bar
4. Once "approved" — mark phase complete in ROADMAP.md and STATE.md
</remaining_work>

<decisions_made>
- All matchup rows bookable (user feedback during verification)
- Single H2H mode removed (user request during verification)
- stage_url optional in bet booking API — synthetic URL from race name for Pinnacle flow
- data/odds.py fully deleted (replaced by data/pinnacle_scraper.py)
- test_odds.py and test_pinnacle_bp.py deleted (stale imports to deleted module; Plan 03 created replacement test_pinnacle_bp.py)
</decisions_made>

<blockers>
- Human verification: 3 UI items need browser testing before phase can be marked complete
- User was running old Flask server when testing Book Bet — needs restart
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-odds-scraping-clv-infrastructure/06-VERIFICATION.md` — verification status, human items
2. `.planning/phases/06-odds-scraping-clv-infrastructure/06-CONTEXT.md` — all 29 implementation decisions
3. `CLAUDE.md` — project conventions and current best model config

## Infrastructure State
- Flask app needs restart to pick up stage_url fix (commit 214f260)
- 107 tests passing (pytest tests/ -v)
- data/scrape_log.jsonl created during Pinnacle load testing (untracked)

<context>
Phase 6 is functionally complete. The code works — user confirmed Load from Pinnacle was "awesome". The only remaining step is human verification of the booking flow (after server restart) and CLV dashboard, then marking the phase complete. If verification passes, run the update_roadmap step to mark phase 6 complete and advance STATE.md to phase 7.
</context>

<next_action>
Start with: Restart Flask server (`python webapp/app.py`), then test:
1. Book Bet flow — load Pinnacle markets, try booking a bet, confirm dialog works
2. CLV dashboard — open /pnl, verify cards/chart/table render
3. Snapshot capture — click Capture Snapshot, verify status bar

Then say "approved" to mark phase complete, or describe issues.
</next_action>
