---
phase: 06-odds-scraping-clv-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - webapp/pinnacle_bp.py
  - webapp/app.py
  - tests/test_pinnacle_bp.py
autonomous: true
requirements:
  - ODDS-04
  - CLV-06
  - BET-01

must_haves:
  truths:
    - "Pinnacle blueprint uses new Playwright scraper instead of broken guest API"
    - "Model predictions are computed on all scraped matchups at snapshot time and stored alongside odds"
    - "CLV summary and terrain CLV are exposed via JSON API endpoints"
    - "Bet booking API endpoint accepts recommended_stake and writes enriched bet records"
    - "Bet history endpoint supports status, race_name, stage_type filters"
    - "All new API endpoints have _require_localhost decorator"
  artifacts:
    - path: "webapp/pinnacle_bp.py"
      provides: "Rewired blueprint using pinnacle_scraper with snapshot-save and prediction endpoints"
      exports: ["pinnacle_bp"]
    - path: "webapp/app.py"
      provides: "New CLV API endpoints and enhanced bet/history endpoints"
      contains: "api_clv_summary"
    - path: "tests/test_pinnacle_bp.py"
      provides: "Updated tests mocking pinnacle_scraper instead of odds module"
      min_lines: 40
  key_links:
    - from: "webapp/pinnacle_bp.py"
      to: "data/pinnacle_scraper.py"
      via: "import scrape_cycling_markets, save_snapshot"
      pattern: "from data.pinnacle_scraper import"
    - from: "webapp/app.py"
      to: "data/pnl.py"
      via: "import get_clv_summary, get_clv_by_terrain, get_total_bankroll"
      pattern: "from data.pnl import"
---

<objective>
Rewire the Flask API layer: update pinnacle_bp.py to use the new Playwright scraper with model predictions at snapshot time, add CLV summary/terrain API endpoints, enhance bet booking and history endpoints with new fields.

Purpose: ODDS-04 requires model predictions stored with snapshots. CLV-06 requires CLV data accessible via API. BET-01 requires enriched bet records. This plan connects the data layer (Plans 01/02) to the frontend (Plans 04/05).
Output: Modified `webapp/pinnacle_bp.py`, `webapp/app.py`, updated `tests/test_pinnacle_bp.py`
</objective>

<execution_context>
@.planning/phases/06-odds-scraping-clv-infrastructure/06-RESEARCH.md
@.planning/phases/06-odds-scraping-clv-infrastructure/06-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/06-odds-scraping-clv-infrastructure/06-01-SUMMARY.md
@.planning/phases/06-odds-scraping-clv-infrastructure/06-02-SUMMARY.md

<interfaces>
From data/pinnacle_scraper.py (Plan 01):
  @dataclass MatchupSnapshot: rider_a_name, rider_b_name, odds_a, odds_b, race_name, race_slug, start_time, start_date, snapshot_type, source_url
  class PinnacleScrapeError(Exception)
  def scrape_cycling_markets(headed: bool = False, snapshot_type: str = "manual") -> list[MatchupSnapshot]
  def save_snapshot(snapshots: list[MatchupSnapshot], db_path: str = DB_PATH) -> None

From data/pnl.py (Plan 02):
  def compute_clv(bet_odds, closing_odds_a, closing_odds_b, selection) -> tuple[float, float]
  def get_total_bankroll(db_path: str = DB_PATH) -> float
  def get_clv_summary(db_path: str = DB_PATH) -> dict  # returns avg_clv, avg_clv_no_vig, ci_low, ci_high, n_bets
  def get_clv_by_terrain(db_path: str = DB_PATH) -> list[dict]  # returns list of {stage_type, n_bets, avg_clv, ...}
  def get_bet_history(db_path, limit, status, race_name, stage_type, date_from, date_to) -> list[dict]
  def place_bet(..., recommended_stake: float = 0.0, ...) -> int
  def get_current_bankroll(db_path: str = DB_PATH) -> float

From data/name_resolver.py:
  class NameResolver: def resolve(self, pinnacle_name: str) -> ResolveResult

From models/predict.py:
  class Predictor: def predict(self, rider_a_url, rider_b_url, ...) -> dict

From webapp/auth.py:
  def _require_localhost(f) -> wrapped function
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire webapp/pinnacle_bp.py to use Playwright scraper with model predictions at snapshot time</name>
  <files>webapp/pinnacle_bp.py, tests/test_pinnacle_bp.py</files>
  <read_first>
    - webapp/pinnacle_bp.py (full file — existing blueprint structure, routes, imports, error handling)
    - tests/test_pinnacle_bp.py (existing test structure — update mocks from data.odds to data.pinnacle_scraper)
    - webapp/app.py (lines 132-216 — api_predict and api_predict_batch for Predictor usage pattern)
    - models/predict.py (first 50 lines — Predictor class interface)
    - .planning/phases/06-odds-scraping-clv-infrastructure/06-PATTERNS.md (pinnacle_bp.py section)
  </read_first>
  <action>
    **Rewrite `webapp/pinnacle_bp.py`** to use the new scraper per D-01:

    **Imports:** Replace `from data.odds import PinnacleAuthError, fetch_cycling_h2h_markets` with `from data.pinnacle_scraper import PinnacleScrapeError, scrape_cycling_markets, save_snapshot, MatchupSnapshot`. Remove `import requests` (no longer needed). Add `from data.pnl import get_pnl_db, get_total_bankroll`. Add `from models.predict import Predictor`. Keep NameResolver, _require_localhost, dataclasses, logging, defaultdict imports.

    **Modify `/api/pinnacle/load` route:**
    - Replace `fetch_cycling_h2h_markets()` with `scrape_cycling_markets()`.
    - Replace `PinnacleAuthError` catch with `PinnacleScrapeError`.
    - Remove `requests.RequestException` catch (no longer relevant).
    - After scraping, group by race_name (existing pattern).
    - For each pair: resolve names via NameResolver (existing pattern).
    - Add model prediction per D-12/ODDS-04: If both riders resolved (rider_a_url and rider_b_url not None), call the app's Predictor (lazy-loaded) to get model_prob, edge. Compute quarter-Kelly recommended stake from get_total_bankroll(). Add to pair dict: `model_prob`, `edge`, `recommended_stake`, `should_bet` (edge > 0.05).
    - Pair dict fields: pinnacle_name_a, pinnacle_name_b, rider_a_url, rider_b_url, rider_a_resolved, rider_b_resolved, best_candidate_a_name, best_candidate_a_url, best_candidate_b_name, best_candidate_b_url, odds_a, odds_b, start_time, model_prob, edge, recommended_stake, should_bet.
    - Return JSON unchanged format: `{"races": [...]}`

    **Add `/api/pinnacle/snapshot` route** (new, POST, _require_localhost):
    - Scrape markets, save to SQLite via save_snapshot(), return `{"saved": N}`.
    - Per D-12: Also compute and store model predictions in the snapshot (model_prob_a, edge_a, recommended_stake_a, model_prob_b, edge_b, recommended_stake_b fields in market_snapshots table). For each snapshot, if both riders can be resolved, run prediction and update the snapshot row.

    **Add `/api/pinnacle/snapshot/closing` route** (new, POST, _require_localhost):
    - Same as snapshot but with snapshot_type="closing". For CLV-01.

    **Lazy Predictor pattern:** Store predictor as a module-level variable, lazy-loaded on first use (same pattern as webapp/app.py). Use `_get_predictor()` helper.

    **Update `tests/test_pinnacle_bp.py`:**
    - Change all mocks from `data.odds.fetch_cycling_h2h_markets` to `data.pinnacle_scraper.scrape_cycling_markets`.
    - Change `data.odds.PinnacleAuthError` to `data.pinnacle_scraper.PinnacleScrapeError`.
    - Remove any references to matchup_id (not used in new scraper).
    - Add test for /api/pinnacle/snapshot endpoint.
  </action>
  <verify>
    <automated>cd B:/ml-cycling-predictor && python -m pytest tests/test_pinnacle_bp.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - webapp/pinnacle_bp.py contains `from data.pinnacle_scraper import` (not `from data.odds import`)
    - webapp/pinnacle_bp.py does NOT contain `fetch_cycling_h2h_markets` or `PinnacleAuthError`
    - webapp/pinnacle_bp.py contains `@pinnacle_bp.route("/api/pinnacle/snapshot"` 
    - webapp/pinnacle_bp.py contains `@_require_localhost` on all route functions
    - webapp/pinnacle_bp.py contains `save_snapshot(` call
    - webapp/pinnacle_bp.py contains `Predictor` import or lazy-load pattern
    - tests/test_pinnacle_bp.py does NOT reference `data.odds`
    - `pytest tests/test_pinnacle_bp.py -x -v` exits 0
  </acceptance_criteria>
  <done>Pinnacle blueprint uses Playwright scraper. Model predictions computed at snapshot time per ODDS-04. Snapshot save and closing snapshot endpoints added.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLV and enhanced bet API endpoints to webapp/app.py</name>
  <files>webapp/app.py</files>
  <read_first>
    - webapp/app.py (lines 389-480 — existing P&L routes, api_place_bet, api_pnl_summary, api_pnl_history patterns)
    - .planning/phases/06-odds-scraping-clv-infrastructure/06-PATTERNS.md (app.py section — CLV endpoint patterns, bet booking pattern)
  </read_first>
  <action>
    **Add imports** at top of webapp/app.py:
    Add `get_clv_summary, get_clv_by_terrain, get_total_bankroll` to the existing `from data.pnl import` line.

    **New endpoint: `/api/pnl/clv-summary`** (GET):
    ```python
    @app.route("/api/pnl/clv-summary")
    def api_clv_summary():
        """Return CLV summary stats with bootstrap CI for P&L page."""
        return jsonify(get_clv_summary())
    ```

    **New endpoint: `/api/pnl/clv-by-terrain`** (GET):
    ```python
    @app.route("/api/pnl/clv-by-terrain")
    def api_clv_by_terrain():
        """Return CLV breakdown by stage type."""
        return jsonify(get_clv_by_terrain())
    ```

    **New endpoint: `/api/pnl/total-bankroll`** (GET):
    ```python
    @app.route("/api/pnl/total-bankroll")
    def api_total_bankroll():
        """Return total bankroll (cash + pending stakes) per D-20."""
        return jsonify({"bankroll": get_total_bankroll()})
    ```

    **Modify existing `/api/pnl/history`** endpoint:
    Add query parameter parsing for filters per D-19/BET-03:
    ```python
    @app.route("/api/pnl/history")
    def api_pnl_history():
        limit = request.args.get("limit", 50, type=int)
        status = request.args.get("status")
        race_name = request.args.get("race_name")
        stage_type = request.args.get("stage_type")
        date_from = request.args.get("date_from")
        date_to = request.args.get("date_to")
        return jsonify(get_bet_history(
            limit=limit, status=status, race_name=race_name,
            stage_type=stage_type, date_from=date_from, date_to=date_to
        ))
    ```

    **Modify existing `/api/pnl/bet`** endpoint per BET-01/D-14:
    Add `recommended_stake` to the place_bet call:
    ```python
    recommended_stake=float(data.get("recommended_stake", 0)),
    ```
    Also add `capture_timestamp` if provided.

    **Modify existing `/api/pnl/auto-settle`** response to include CLV info:
    ```python
    @app.route("/api/pnl/auto-settle", methods=["POST"])
    @_require_localhost
    def api_auto_settle():
        count = auto_settle_from_results()
        return jsonify({"settled": count, "bankroll": get_current_bankroll()})
    ```
    (auto_settle_from_results already computes CLV internally via settle_bet from Plan 02.)

    All new endpoints follow existing pattern: no decorator needed for GET reads, `@_require_localhost` for POST mutations.
  </action>
  <verify>
    <automated>cd B:/ml-cycling-predictor && python -c "from webapp.app import app; client = app.test_client(); resp = client.get('/api/pnl/clv-summary'); print(resp.status_code); assert resp.status_code == 200"</automated>
  </verify>
  <acceptance_criteria>
    - webapp/app.py contains `from data.pnl import` with `get_clv_summary`, `get_clv_by_terrain`, `get_total_bankroll`
    - webapp/app.py contains `@app.route("/api/pnl/clv-summary")`
    - webapp/app.py contains `@app.route("/api/pnl/clv-by-terrain")`
    - webapp/app.py contains `@app.route("/api/pnl/total-bankroll")`
    - webapp/app.py api_pnl_history contains `request.args.get("status")`
    - webapp/app.py api_place_bet contains `recommended_stake`
    - Test client GET /api/pnl/clv-summary returns 200
  </acceptance_criteria>
  <done>All CLV, bankroll, and enhanced bet API endpoints are wired. History endpoint supports filtering. Bet booking endpoint accepts recommended_stake.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Flask API | HTTP requests from localhost browser |
| Flask API -> scraper | Triggering Playwright scrape via API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | webapp/app.py api_pnl_history | mitigate | Filter values passed as parameters to get_bet_history which uses parameterized SQL |
| T-06-07 | Elevation | webapp/pinnacle_bp.py snapshot routes | mitigate | @_require_localhost on all POST endpoints prevents remote triggering |
</threat_model>

<verification>
- `pytest tests/test_pinnacle_bp.py -x -v` passes
- Flask test client returns 200 for /api/pnl/clv-summary
- All new endpoints use correct decorators
</verification>

<success_criteria>
- Pinnacle blueprint fully rewired to Playwright scraper
- Model predictions stored at snapshot time
- CLV summary and terrain endpoints return JSON
- Bet history supports query filters
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-odds-scraping-clv-infrastructure/06-03-SUMMARY.md`
</output>
